# DoIP Simulated Entity Example v3

This example shows how to use the DoIP v3 protocol to talk to an ECU by simulating an entity and sending a UDS response frame to a tester.

To create the required certificates and keys, run `generate-doip-certs.ps1`.

