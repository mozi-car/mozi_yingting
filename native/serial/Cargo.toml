[package]
name = "serial"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "3"
napi-derive = "3"
[target.'cfg(windows)'.dependencies]
serialport = { version = "4", default-features = false }

[build-dependencies]
napi-build = "2"
